# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Local env files
.env
.env.local
.env.development
.env.production
.env.*
!.env.example

# Logs
*.log

# TypeScript
*.tsbuildinfo

# Expo
expo-env.d.ts

# Native project folders — generated by `expo prebuild` from
# app.json. Treat as build output (regenerated on demand for
# `expo run:ios` / `expo run:android` / EAS builds). Commit
# selectively only if you start editing Info.plist / Podfile
# / Gradle directly.
ios/
android/

# Local CocoaPods CDN workarounds (agent/dev only)
.cocoapods-specs/
ios/.cp-home/
ios/.cocoapods-specs/
ios/pod-install.log
ios/expo-run.log
