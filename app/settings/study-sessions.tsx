import { AppBlocksScreen } from "@/components/AppBlocksScreen";

/** Stack entry — same App Blocks UI with a back chevron. */
export default function AppBlocksSettingsScreen() {
  return <AppBlocksScreen showBack />;
}
