/**
 * Shared layout tokens — Espinal / Apple HIG grouped-screen inset.
 *
 * Use SCREEN_H_PAD (20pt) for page-level horizontal padding on tab
 * screens and cards. SettingsScaffold uses Tailwind px-5 (also 20pt).
 */

/** Standard horizontal inset for tab screens and grouped cards. */
export const SCREEN_H_PAD = 20;
